# gradle

## gradlew

```bash
./gradlew tasks

./gradlew subprojectName:tasks --all # с --all tasks will be displayed, including minor ones

./gradlew app:assembleDevelopDebug # calling a task with dependencies
./gradlew app:assembleDD # short alternative


```